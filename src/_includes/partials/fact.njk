<p class="fact" style="--count-to: {{ fact.number }}; --length: {{ fact.number | length }}">
  <span class="fact__label" aria-label="{{ fact.number }} {{ fact.label }}">
    {{ fact.label }}
  </span>
</p>